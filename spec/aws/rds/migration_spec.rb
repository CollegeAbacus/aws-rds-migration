RSpec.describe Aws::Rds::Migration do
  it "has a version number" do
    expect(Aws::Rds::Migration::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
